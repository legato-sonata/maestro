{
  "name": "playwright-codespace-poc",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "record": "pulseaudio --start && xvfb-run --auto-servernum node --env-file=maestro.env record.js"
  },
  "dependencies": {
    "playwright": "latest"
  }
}
